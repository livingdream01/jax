services:
  apex-server:
    build:
      context: ./server
      dockerfile: Dockerfile
    ports:
      - "3001:3001"
    environment:
      - PORT=3001
      - OPENROUTER_API_KEY=${OPENROUTER_API_KEY:-}
      - TAVILY_API_KEY=${TAVILY_API_KEY:-}
      - NEWSAPI_KEY=${NEWSAPI_KEY:-}
      - GNEWS_KEY=${GNEWS_KEY:-}
      - NODE_TLS_REJECT_UNAUTHORIZED=0
    volumes:
      - ./data:/app/data
    restart: unless-stopped

  apex-client:
    build:
      context: ./client
      dockerfile: Dockerfile
    ports:
      - "5173:5173"
    depends_on:
      - apex-server
    restart: unless-stopped

  n8n:
    image: n8nio/n8n:latest
    ports:
      - "5678:5678"
    environment:
      - N8N_BASIC_AUTH_ACTIVE=false
      - N8N_HOST=localhost
      - N8N_PORT=5678
      - N8N_PROTOCOL=http
      - WEBHOOK_URL=http://localhost:5678
    volumes:
      - ./data/n8n:/home/node/.n8n
    restart: unless-stopped

  # Leon voice assistant — uncomment to enable offline voice processing
  # leon:
  #   image: leonai/leon:latest
  #   ports:
  #     - "1337:1337"
  #   environment:
  #     - LEON_LANG=en
  #     - LEON_TELEMETRY=false
  #   volumes:
  #     - ./data/leon:/home/leon/data
  #   restart: unless-stopped